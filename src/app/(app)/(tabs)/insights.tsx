import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function InsightsScreen() {
  return (
    <PlaceholderScreen
      title="Insights"
      description="Your full insights feed — every AI-generated recommendation in one place — is coming soon."
      symbol="chart.line.uptrend.xyaxis"
      fallbackGlyph="▲"
    />
  );
}
